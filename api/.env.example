# Comma-separated custom browser origins allowed to call this API.
# Vercel *.vercel.app deployments are also allowed by the API.
CORS_ORIGINS=https://plantly-pred.vercel.app
PORT=8000
PYTHON_VERSION=3.12.8
